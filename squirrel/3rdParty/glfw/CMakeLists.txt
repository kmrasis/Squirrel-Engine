add_library(dep_glfw STATIC IMPORTED GLOBAL)
set_target_properties(dep_glfw PROPERTIES
    IMPORTED_LOCATION "${CMAKE_CURRENT_LIST_DIR}/libglfw_v3.4.a"
    INTERFACE_INCLUDE_DIRECTORIES "${CMAKE_CURRENT_LIST_DIR}/include"
)
